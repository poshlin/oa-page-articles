# 橘蘋觀點 Journal — 視覺規範（DESIGN.md）

> **本檔不是獨立的設計系統。** 色彩、字體、間距、圓角、元件一律**繼承橘子蘋果程式學苑 Design System SSOT**
> （`oa-page-home/DESIGN.md`）。本檔只記載「橘蘋觀點」這個**內容媒體子品牌**經核可的差異，以及本頁的紅線註記。
> 任何 AI agent 修改本 repo 的 UI 前，先讀 SSOT，再讀本檔。
>
> v1｜2026-07-24｜配色由自訂墨綠系遷移回品牌 token 時建立。

---

## 一、繼承自 SSOT（不得在本 repo 覆寫）

- **品牌亮橘 `#FFA300`**：唯一主角色，不變暗、不土色、不配白字。
- **中性色**：`--ink #12151E`、`--text #2D2E32`、`--muted #5B616D`、`--line #E6E8EC`、`--warm #FAF9F6`。
- **輔色**：teal `#00C4B3`（文字用 teal-ink `#00776B`）、coral `#FF6F61`（文字用 `#D64A3D`）。
- **圓角**：8 / 16 / 24 / pill 999px。按鈕一律膠囊，卡片 16，大面板 24。
- **免費體驗 CTA ＝ button-primary**：亮橘實心 ＋ 深墨字 `#2D2E32` ＋ 膠囊 ＋ `border: 2px solid transparent`。
- **WCAG**：內文 ≥4.5:1、大字（≥18.66px 粗體或 24px）≥3.0:1。

### 本 repo 額外定義的兩個中性階（僅補 SSOT 之不足，非新品牌色）
| token | 值 | 用途 |
|---|---|---|
| `--line-strong` | `#CBD0D8` | 需要比 `--line` 更明顯的邊框（輸入框外框、卡片 hover 邊） |
| （靛淺底） | `#E9EEF8` | SSOT 的 navy-light，用於第 4 組 quick-link chip |

---

## 二、經核可的子品牌差異（Journal Exception）

橘蘋觀點是**內容媒體**，不是銷售頁。目的是建立「可被信任的教育媒體」觀感，因此核可下列兩項與 SSOT 的差異：

### 1. 襯線標題（Noto Serif TC）
- **允許範圍**：僅限文章／專題**標題層**——`.issue-card h2`、`.feature-card` 標題、`.topic-panel h3`、`.trial-cta h2`。
- **禁止範圍**：內文、UI 標籤、按鈕、導覽、任何功能性文字一律 **Noto Sans TC**。
- **理由**：襯線標題是雜誌型媒體的可信度訊號；限縮在標題層即可取得該訊號，又不動搖全站字體一致性。
- 兩套字體皆以 webfont 載入（見 `index.html` 的 Google Fonts link），確保各裝置一致，不依賴使用者本機是否安裝。

### 2. 亮橘當「標記」而非「文字」
- Hero H1 的強調段落 `<em>`：**文字用 `--ink`**，亮橘以**螢光筆色帶**呈現
  （`linear-gradient(transparent 64%, rgba(255,163,0,.40) 64%)`）。
- **理由**：亮橘對白底僅 1.9:1，永遠不能當大字文字；深橘 `#9A5A00` 在 66px 會讀成土色。
  改用色帶後，文字維持 13.5:1，亮橘仍作為視覺鉤子出現，且符合「亮橘只做強調、不稀釋」。
- **通則**：本頁任何「想用橘色強調文字」的需求，一律改成底線／色帶／左側色條，**不改文字顏色**。

---

## 三、本頁紅線註記

- `data-oa-cta` **只**出現在 `.trial-cta` 的 `<button>`，值為位置短代號（目前 `bottom`）。
  快速探索 chip、分類卡、錨點一律不得加。
- 全站禁用彩色 emoji；需要圖示時用單色字符（`→ × ⌄`）。
- 對外數字與課程資訊一律出自知識庫，本頁不得自行編寫。
- 品牌全名寫「**橘子蘋果程式學苑**」，「學院」為錯字。

---

## 四、修改前自檢

1. 新增顏色？→ 先問「SSOT 有沒有對應 token」。沒有才考慮，且需回寫本檔。
2. 想讓某段文字變橘？→ 停。改用色帶／底線／色條（見二之 2）。
3. 動到 CTA？→ 對比必須 ≥4.5:1，且維持亮橘實心＋深墨字＋膠囊。
4. 改完跑一次：殘留野生 hex 掃描 ＋ WCAG 配對計算 ＋ 桌機／手機各一張截圖。
